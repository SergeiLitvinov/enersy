# render.yaml
services:
  # --- Frontend (React + Vite + Rust/WASM) ---
  - type: web
    name: frontend
    region: oregon
    plan: free
    env: docker
    dockerfilePath: frontend/Dockerfile
    # buildCommand: cd rust-wasm && wasm-pack build --target web --out-dir ../frontend/public/rust-wasm && cd ../frontend && npm install && npm run build
    # startCommand: cd frontend && npx serve -s dist
    envVars:
      - key: PORT
        value: 10000

  # --- Backend Go ---
#  - type: web
#    name: backend-go
#    region: oregon
#    plan: free
#    env: docker
#    dockerfilePath: backend-go/Dockerfile
#    envVars:
#      - key: PORT
#        value: 8080

  # --- Backend Python ---
#  - type: web
#    name: backend-python
#    region: oregon
#    plan: free
#    env: docker
#    dockerfilePath: backend-python/Dockerfile
#    envVars:
#      - key: PORT
#        value: 8000

  # --- Backend Julia ---
#  - type: web
#    name: backend-julia
#    region: oregon
#    plan: free
#    env: docker
#    dockerfilePath: backend-julia/Dockerfile
#    envVars:
#      - key: PORT
#        value: 8081

  # --- PostgreSQL Database ---
#  - type: postgres
#    name: my-postgres
#    plan: free
#    region: oregon
#    databaseName: myapp
#    user: myuser
#    password: mysecretpassword
#    initScript: database/init.sql
